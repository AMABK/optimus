import { NgModule } from '@angular/core';
import { DynamicTreeComponent } from './dynamic-tree.component';

@NgModule({
  declarations: [DynamicTreeComponent],
  imports: [
  ],
  exports: [DynamicTreeComponent]
})
export class DynamicTreeModule { }
